# クレジット失効日までの実行計画（Day 4 以降の正本。#103）

[day3-5-execution-plan.md](./day3-5-execution-plan.md) の **§4（Day 4）/ §5（Day 5）を置き換える後継文書**。
方針転換の判断は [ADR-0020](../adr/0020-credit-window-resource-strategy.md)。
旧計画の §1〜§3（Day 3。実施済み）、§2-1 / §2-2 の No. 付き事実表、§7〜§9 は引き続き有効で、
本書から参照する（事実表は多数の ADR から参照されており、移動すると導線が壊れるため動かさない）。

> 実行前提: apply / destroy / az 書き込みは CLAUDE.md の禁止事項に従い**ユーザーの明示承認を
> 得てから**実行する。本書は計画の正本であって実行許可ではない。

## 0. 前提の整理 — 何が変わり、何が変わらないか

- **変わった前提**: クレジットは期限付き（§1-1）で、使わなかった分は失効して消える。
  「課金を極力抑える」は節約ではなく、期限付きリソースを前にした遠慮だった
- **変わらない前提**: **目的は証跡の取得であり、クレジットは手段**。余らせて失効しても損失は
  ゼロだが、**使い切ると spendingLimit: On のためサブスクリプションが無効化され、PostgreSQL を
  含む全リソースが停止**する。ドリル中に踏めば計測記録ごと失われる。**金より証跡が高い**
- したがって本計画は「使い切る」計画ではなく、**上限（§8）とガードレール（§9）の内側で、
  時間をかけないと取得できない証跡を取り切る**計画である

### 証跡の 2 分類（本計画の骨格）

| 分類 | 性質 | 扱い |
| --- | --- | --- |
| **(a) 時間が価値を生む観測** | autovacuum の自然発火、bloat の時系列、閾値レンジ、PITR 復元ウィンドウの拡大、バックアップ/WAL の推移、可用性 SLI、計画メンテ遭遇 | **動かし続ける**（約 1.0〜2.1 USD/日 = §1-3。上限内に収まる。削るのは最後） |
| **(b) イベントの計測** | B1ms→GP スケール、HA 有効化/無効化、planned / forced failover | **時間を区切る**。HA を何日置いてもフェイルオーバーの計測値は良くならない。**測ったら戻す** |

なぜ後継文書か: 旧計画は「Day 4 / Day 5」の日単位詰め込みが骨格で、本書は「(a) の期間観測 +
(b) の時間を区切ったイベント計測」が骨格。章構成ごと変わるため旧文書内改訂では差分が読めない。
旧計画の §2-1（No.1〜31）/ §2-2 は出典と実測の台帳として残す。

## 1. 確定した事実（すべて実測。取得日時・取得コマンド付き）

### 1-1. クレジットの失効日（本計画の締め切り）

Microsoft.Consumption lots API（2026-08-22 13:10:57Z 取得。billing account / profile 名は CLI で
都度取得、コマンドは旧計画 §8）:

| 項目 | 実測値 |
| --- | --- |
| source / originalAmount | Azure sign-up credit / 200.00 USD |
| startDate | 2026-08-19T06:59:34Z |
| **expirationDate** | **2026-09-18T06:59:34Z**（JST 9/18 15:59） |
| balanceSummary（同時刻） | current 200.00 / estimated 199.83 USD |

- 残り: 本日 2026-08-22 から**約 26.7 日**
- `FreeTrial_2014-09-01` / `spendingLimit: On`（2026-08-22 実測）: 超過請求は発生しない代わりに、
  **使い切った時点で全停止**する（上の「金より証跡」の根拠）
- **課金データには反映ラグがある**（2026-08-22 実測: 09:00〜13:10Z の稼働分が balanceSummary に
  未反映のまま）。**見積もりと残高表示を信じきらない**こと（ガードは §9）

### 1-2. 単価（Retail Prices API、japaneast、2026-08-22 実測）

| メーター | 単価 | 日額換算 |
| --- | --- | --- |
| PostgreSQL Flexible **B1MS** compute | 0.026 USD/時 | 0.624 USD/日（12か月無料枠 750h/月 の対象 = 台帳） |
| PostgreSQL Flexible **GP Ddsv5 2 vCore** compute | 0.244 USD/時 | 5.856 USD/日 |
| PostgreSQL Flexible Storage Data Stored | 0.138 USD/GiB/月 | 32 GiB で 0.145 USD/日 |
| Backup Storage LRS | 0.095 USD/GB/月 | 現在の実測使用量（~5.4 MiB ×2）では誤差 |
| ACA Standard vCPU Active / Idle | 0.000024 / 0.000003 USD/秒 | ops 常駐: active 0.648 / idle 適用時 0.194 USD/日 |
| ACA Standard Memory Active / Idle | ともに 0.000003 USD/GiB 秒 | 同上に含む |

- **HA の standby は primary と同額課金**。出典（[Reliability in Azure Database for PostgreSQL](https://learn.microsoft.com/en-us/azure/reliability/reliability-database-postgresql)、逐語）:
  "When you enable high availability, a standby server is created and it's billed at the same
  rate as the primary server. The availability zone configuration doesn't affect the cost. There
  are no charges for data replication within or between availability zones."
  standby は "deployed in the same VM configuration as the primary server, including vCores,
  storage, and network settings" のため storage も同量
  → **GP 2vCore + zone-redundant HA ≒ 12.0 USD/日**（5.856×2 + 0.145×2）
- **GP は 12か月無料枠の対象外**（無料枠は B1MS 750h/月 + 32GB storage + 32GB backup =
  [台帳](./azure-resource-inventory.md)の記録が正本。公式ページ再確認は §6 の実行前チェックに含める）
- HA は Burstable 非対応（同 Reliability ページの表）→ HA 計測には GP 昇格が前提（旧計画どおり）

### 1-3. (a) 常時稼働ベースラインの日額

| 項目 | 日額 | 根拠 |
| --- | --- | --- |
| ACR + CAE managed resources | 約 0.84 USD | ADR-0018 追記 #84（Retail Prices API 実測単価） |
| ops 常駐（min_replicas 1） | 0.194〜0.648 USD | idle 適格 6 条件充足は実測済み、**単価の実適用は未確認**（ADR-0015 追記）。実適用は稼働 2〜3 日目の課金反映で確認し、本表を実測値に更新する |
| PostgreSQL B1ms | 0〜0.624 USD | 無料枠 750h/月 内なら 0（private access での適用は台帳で「未確定」のまま。実課金で確認） |
| serving / マーカー Job / スナップショット採取 / probe | ≒ 0〜0.1 USD | serving はスケールゼロ実測。Job は §5-3 の見積。ACA 月次無料枠（180,000 vCPU 秒 + 360,000 GiB 秒 + 200 万リクエスト）が先に吸収 |
| **合計** | **約 1.0〜2.1 USD/日** | 26.7 日で 27〜56 USD。期待値 ~1.5 USD/日 ≈ 40 USD |

## 2. スケジュール（日付は目安。失効 9/18 06:59Z が唯一の硬い締め切り）

| 期間 | フェーズ | 内容 |
| --- | --- | --- |
| 8/23〜8/24 | §4 仕込み | **/chat 保護（#107。(a) 開始の前提条件 = 先行ゲート）** → マーカー + スナップショット採取 #104 / 予算アラート #105 / 外形監視 #106 |
| 8/25〜8/26 | PITR ドリル 1 回目 | 旧計画 §4-3 の手順のまま（変更点は §7-1） + 合成メンテナンステスト（旧計画 §4-6） |
| 8/25〜9/12 | §5 期間観測 (a) | 日次チェック + §5-1 の観測群。**本計画の主役** |
| 9/9〜9/10 の 2 日間 | §6 イベント計測 (b) | B1ms→GP → HA 有効化 → failover 2 種 → HA 無効化 → GP→B1ms。**測ったら戻す** |
| 9/11〜9/12 | PITR ドリル 2 回目 | 復元ウィンドウが 7 日超に拡大した状態（§5-1 の 4）で、24 時間以上前への復元 |
| 9/15〜9/16 | §7 最終 teardown | 証跡の締め → ephemeral → persistent の順に destroy。失効まで 2 日のマージン |

- (b) を 9/9〜9/10 に置く理由: (a) の観測が 2 週間分たまり、かつ teardown まで 5 日の余裕を
  残せる位置。B1ms に戻した後も (a) の観測は 9/12 まで続く（GP 期間の前後比較が取れる）

## 3. 「クレジットに余裕があるからこそ取れる観測」の採否（すべて (a)）

| # | 観測 | 採否 | 根拠 / 方法（採取の実装は #104） |
| --- | --- | --- | --- |
| 1 | autovacuum の**自然**発火 | **採用** | 合成では絶対に取れない（発火の瞬間を狙って作る合成と、閾値 `0.2×N+50` を実ワークロードが自然に越える観測は別物）。`pg_stat_user_tables` の `n_dead_tup` / `last_autovacuum` / `autovacuum_count` をスナップショット採取 |
| 2 | bloat の時系列（鋸歯） | **採用** | `pgstattuple` の定期採取。1 点の測定より説得力が桁違い。vacuum 後に落ちる波形まで取る |
| 3 | 監視閾値の実測レンジ | **採用** | CPU / メモリ / 接続数 / ストレージ / IOPS の分布から「p95 + マージン」で閾値を決める（旧計画 §7 の要求そのもの）。Azure Monitor メトリクスは Azure 側に 93 日保持されるため採取は日次エクスポートで足りる |
| 4 | PITR 復元ウィンドウの拡大 | **採用** | サーバー再作成直後の今は幅がほぼない。**8/29 頃に保持 7 日の窓が満杯になり `earliestRestoreDate` が動き出す**（この「窓がスライドし始める瞬間」自体が時間でしか取れない観測）。2 回目ドリルで 24 時間以上前へ復元 |
| 5 | バックアップ使用量の推移 | **採用** | 日次チェックで記録済みの項目を継続 + 無料枠 32GB との突き合わせ。保持 7 日（ADR-0011）の決定根拠を実測で裏付ける |
| 6 | WAL 生成レート | **採用** | PG17 の `pg_stat_wal`（`wal_records` / `wal_bytes`）をスナップショット採取。継続書き込み下の生成レート → 容量計画・バックアップサイズの話につなぐ |
| 7 | 可用性・レイテンシ SLI | **採用** | #106 の外形監視。コールドスタート 22.7 秒は 1 回きりの観測で**再現性未確認** → 分布を取る |
| 8 | 計画メンテナンス遭遇 | **採用（受動）** | 数日動かして当たれば「メンテ中に止まりましたか」への実測回答。当たらなければ**当たらなかったと記録**する。maintenance window 設定値と Service Health を日次チェックに含める |
| 9 | 「何も起きなかった」記録 | **採用** | 安定稼働 N 日・probe 成功率・Job 成功率として定量化（沈黙ではなく数字で残す） |
| 10 | XID 消費と frozen age の推移 | **採用（追加。候補外）** | `age(datfrozenxid)` の進み方を継続書き込み下で記録。wraparound 対策（autovacuum の第二の役割）を実データで語れる。採取は 1 行の SQL で追加コストほぼ 0 |
| 11 | Log Analytics 取込量の推移 | **採用（追加。候補外）** | ADR-0016 が「放置時の総額は実測していない」と明記した未実測項目。常時稼働はこれを実測する唯一の機会（`Usage` テーブルを日次確認） |

## 4. 仕込みフェーズ（Issue 起票済み）

**依存関係: #107（/chat 保護）の決定と実装が完了するまで、(a) の常時稼働は開始しない。**
無認証の LLM エンドポイントを 24/7 で晒すことは、理屈上**青天井の課金経路**を開けたまま
無人運転することであり、課金反映ラグ（§1-1）のため事後検知も遅れる。選択肢は §10-1。

1. **/chat 保護（#107）** — 上記ゲート。ユーザー判断待ち
2. **マーカー書き込み + 観測スナップショット採取（#104）** — 設計は §5-3
3. **予算アラート（#105）** — 閾値は §8 の上限に対する 50% / 75% / 90% の 3 段 + 予測ベース。
   FreeTrial（invoice が立たない）で Budget がクレジット消費を捉えるかは**未実測** — #105 の
   最初の作業として実測し、捉えない場合は §9 の日次チェック（balanceSummary）を一次見張りに
   格上げする
4. **外形監視（#106）** — /readyz の 5 分間隔 probe + **鮮度ゲート**（§5-3 の無音失敗対策）

## 5. 期間観測 (a)（8/25〜9/12）

### 5-1. 観測項目

§3 の採否表のとおり（1〜11 すべて）。

### 5-2. 日次チェック（終業時 5 分。旧計画 §8 の見張りに追加）

```bash
# コスト（反映ラグあり = 前日以前の確定分を見る、と明記して記録する）
#   balanceSummary（旧計画 §8 のコマンド）→ estimated の日次差分を記録
# バックアップ / 復元ウィンドウ
az postgres flexible-server show -g rg-felisaichatbot-dev-tf -n pgsql-felisaichatbot-dev \
  --query "{earliest: backup.earliestRestoreDate, state: state}" -o json
# 採取ジョブの生存（無音失敗の一次防衛。§5-3）
az containerapp job execution list -g rg-felisaichatbot-dev-tf -n <marker-job> \
  --query "[?properties.status!='Succeeded'] | length(@)"
# probe 成功率は GitHub Actions の失敗通知 + 週次集計
# Service Health / 計画メンテ（§3 の 8）
az postgres flexible-server show -g rg-felisaichatbot-dev-tf -n pgsql-felisaichatbot-dev \
  --query "maintenanceWindow" -o json
```

### 5-3. 採取の設計（#104 の実装仕様。根拠つき）

- **書き込み（観測対象を作る側）**: マーカー INSERT 1 行/分 + カウンタ行 UPDATE 1 回/分。
  dead tuple の供給源はこの UPDATE（1,440 dead tuples/日）。マーカーテーブルの
  autovacuum 閾値（0.2×N+50）は N の増加とともに上がるため、**発火間隔が日を追って延びる
  過程そのものが §3-1 の観測になる**
- **スナップショット採取（観測する側）**: 同じ cron Job 実行内で `pg_stat_user_tables` /
  `pg_stat_wal` / `age(datfrozenxid)` / DB サイズを **5 分間隔**で観測用テーブル
  （INSERT-only）へ保存。`pgstattuple` のみ **1 時間間隔**
  - 5 分の根拠: `last_autovacuum` / `autovacuum_count` は正確な時刻を持つため、採取は発火を
    「挟めれば」よく、1 分採取は情報を増やさない。粗すぎると `n_dead_tup` の鋸歯の立ち上がりを
    潰す。5 分で 1 日 288 点 = 発火前後の形が十分残る
  - `pgstattuple` を 1 時間にする根拠: 実テーブルのフルスキャンを伴う観測で、**観測が観測対象に
    与える影響が最大の項目**（バッファ・I/O）。bloat の変化スケール（時間〜日）に対して 1 時間で
    十分な分解能
- **採取先は DB 内の観測用テーブル**（Log Analytics ではなく）。根拠: (i) SQL でそのまま
  時系列分析でき、成果物の証跡としてダンプも取れる、(ii) Log Analytics は取込課金と
  スキーマ設計が増えるうえ、DB 内部統計を外に出す変換層が要る、(iii) 観測テーブル自体が
  バックアップサイズ推移（§3-5）の一部になる。**トレードオフ**: PITR ドリルで復元した
  サーバー側の観測テーブルは復元時点までしか持たない（ドリル記録は従来どおり
  `docs/verification/` に別途残すので問題ない）
- **観測者効果の明記**: 採取自体も書き込み（5 分ごと数行 INSERT + 1 時間ごとのフルスキャン読み）
  であり、WAL / バックアップ / bloat の測定値に採取分が混入する。観測用テーブルを専用スキーマに
  分離し、テーブル別の統計（`pg_stat_user_tables` はテーブル単位）で**マーカー系と観測系を
  分けて記録**することで、混入を定量化可能にする
- **無音失敗の防止（最悪 =「3 日前から止まっていた」）**: 3 層で守る
  1. Job execution の失敗は日次チェック（§5-2）で数える
  2. **鮮度ゲート**: /readyz の応答に「最新マーカーの経過秒」を含める改修（#104/#106 で設計）を
     入れ、外形監視が **鮮度 10 分超で workflow を fail** させる → GitHub からの失敗通知で
     即日検知（probe の生存確認と採取の生存確認を 1 本に束ねる）
  3. 週次の集計（probe 成功率・Job 成功数）で取りこぼしを拾う

## 6. イベント計測 (b)（9/9〜9/10 の 2 日間。測ったら戻す）

順序と各計測（手法は旧計画 §5 のまま。1 秒間隔疎通ループ + #106 の外形監視の両方で取る）:

1. 実行前チェック: `plan -detailed-exitcode` exit 0 / GP の無料枠除外の公式再確認 /
   クレジット残の確認（§9 の打ち切り基準を先に確認）
2. **B1ms → GP 2vCore スケール**（ダウンタイム実測。旧計画 §2-2 No.4）
3. **zone-redundant HA 有効化**（所要時間実測。旧計画 §2-2 No.5）
4. **planned failover** → 復帰確認 → **forced failover**（ダウンタイム実測。旧計画 §2-2 No.6/7）。
   実測値を SLA（zone-redundant 99.99% / HA なし 99.9% = §1-2 の Reliability ページ）と
   突き合わせ、「SLA が保証しないもの（≠ RTO/RPO）」を証跡にする
5. **HA 無効化 → GP → B1ms へ戻す**（戻し方向のダウンタイムも実測。Burstable は HA 非対応の
   ため必ず HA 無効化 → スケールダウンの順）
6. 前後の (a) 観測は止めない（GP 期間中のメトリクスレンジが §3-3 の比較点として残る）

- コスト: GP+HA 12.0 USD/日 × 2 日 = **24 USD**（+ B1ms↔GP の遷移日は按分）。
  **2 日で足りる根拠**: (b) はすべてイベント計測で、上記 4 種 + 予備再試行を 2 日に収められる。
  HA を長く置いても failover の計測値は改善しない（§0）。予備日は §2 のとおり 9/11〜9/12 に
  PITR 2 回目としてスケジュール済みで、(b) が延びた場合はそこを食う（teardown は動かさない）

## 7. PITR ドリル（2 回。手順の正本は旧計画 §4-3 のまま）

### 7-1. 変更点のみ

- マーカーは #104 の常時書き込みの**実履歴**を使う（ドリルのために回し始めない）
- 1 回目（8/25〜26）: 復元ウィンドウが浅い状態での基準計測 + 合成メンテナンステスト
  （旧計画 §4-6。**合成は残す** — 狙って再現できるのは合成だけで、阻害条件は 2026-08-19 に
  ローカル実測済み。§3 の自然発火観測と**代替でなく補完**）
- 2 回目（9/11〜12）: **24 時間以上前**への復元（§3-4 の窓拡大後）。B1ms に戻した後の実施
- メトリクス読み取りは **end-time 明示**（#102 で実測した偽 0 の罠）

## 8. 消費上限（ガードレール。ユーザー承認待ち = §10-2）

積み上げ（§1-3 / §6 / OpenAI）:

| 項目 | 期待 | ワースト |
| --- | --- | --- |
| (a) ベースライン 26.7 日 | 40 USD（1.5/日） | 56 USD（2.1/日） |
| (b) GP+HA 2 日 + 遷移 | 24 USD | 30 USD（再試行込み） |
| Azure OpenAI（保護後の RAG 検証・ドリル） | 5 USD | 10 USD |
| **合計** | **69 USD** | **96 USD** |

- **提案: 総消費上限 75 USD（残高の約 37%）**。期待値 69 に対しバッファ 6、ワースト 96 は
  上限超過 = そのときは §9 の打ち切り順で (b) 再試行と OpenAI を削る。coordinator 案の
  60 USD（30%）にする場合は (b) の予備再試行を放棄し OpenAI を 5 に固定すれば収まる
  （60 と 75 の差は「(b) の再試行余地」の値段）— **どちらにするかはユーザー判断（§10-2）**
- Budget アラート（#105）は採択された上限に対し **50% / 75% / 90%** + 予測 100% の 4 段

## 9. 打ち切り基準と失効前手順

**打ち切りの優先順位（消費が想定を超えたとき。上から順に止める）**:

1. **(b) を止める**: HA 無効化 → B1ms へ戻す（イベント計測は途中でも、取れた分が証跡）
2. **Azure OpenAI 経路を止める**: /chat 無効化（保護方式によらず環境変数で塞げる設計を #107 に含める）
3. **(a) は最後まで守る**: それでも足りない場合のみ ephemeral 縮退（ops min 0 化 → 常駐停止）。
   PostgreSQL と採取済みデータ（＝証跡）は最後の最後まで残す

発動条件: 日次チェックで「estimated の日次差分 × 残日数 + 既消費」が上限を超える見込みに
なったとき（課金ラグがあるため **2 日連続で超過見込み**を発動条件にする — 1 日分のラグ起因の
振れで (b) を誤って中断しない）。

**失効日が近づいたときの手順**（9/15〜: 証跡を取り切ってから畳む）:

1. 9/15: 観測データの最終エクスポート（観測テーブルのダンプ → `docs/verification/` 証跡化、
   probe ログ集計、Azure Monitor メトリクスの必要範囲エクスポート）
2. 9/16: ephemeral destroy → persistent destroy（旧計画 §5-6 の手順） → 残存リソースゼロ確認
3. 9/17: 課金反映ラグの分を待って最終コスト実測を記録（これも FinOps 証跡）

- マージン 2 日の根拠: teardown 失敗時の調査・再実行の余地 + 反映ラグ待ちで「最終消費額」を
  確定させるため。失効後はサブスクリプション無効化で**記録の取得自体ができなくなる**

## 10. ユーザー判断待ちの論点

### 10-1. /chat 保護の方式（#107。(a) 開始の先行ゲート）

| 案 | 内容 | 追加コスト | 実装量 | 備考 |
| --- | --- | --- | --- | --- |
| **(a) アプリ層 API キー（推奨）** | FastAPI ミドルウェアで /chat のみ `X-API-Key` 検証。キーは .env / ACA secret | 0 | 小（数十行 + テスト） | /readyz は無認証のまま = 外形監視と両立。デモはキー共有で可 |
| (b) ingress IP 制限 | ip_security_restriction で自宅 IP のみ許可 | 0 | 小（terraform のみ） | GitHub Actions probe（IP 可変）が**通らない** — #106 と両立しない |
| (c) アプリ層レート制限 | slowapi 等で /chat を流量制御 | 0 | 中 | 認証なしでは「ゆっくり課金される」だけ。(a) への追加としてのみ意味がある |
| (d) /chat 無効化フラグ | 環境変数で /chat を 404 に | 0 | 小 | RAG 実測が期間中できなくなる。ただし**打ち切り基準 §9 の 2 で使うため、(a) と併設で実装する価値がある** |
| (e) 外部 ingress 停止 | internal ingress 化 | 0 | 小 | /readyz 外形監視も死ぬ — #106 と両立しない |

**推奨: (a) + (d) 併設**（保護は API キー、緊急遮断はフラグ）。理由: LLM 課金経路だけを塞ぎ、
成果物要件（外形監視・RAG 実測）を全部残しつつ、§9 の打ち切りスイッチも手に入る。

### 10-2. 総消費上限（§8）

**推奨: 75 USD**（根拠は §8 の積み上げ）。60 USD 案との差は (b) の再試行余地。

### 10-3. 決定済み（本計画で結論を出した論点）

- (b) は**時間を区切る**（2 日。§6 に根拠）— HA の多日常駐案は棄却（時間が計測値を良くしない）
- 合成テストと自然発火観測は**併用**（§7-1。代替でなく補完）
- 閾値レンジの収集期間は **2 週間強（8/25〜9/12。GP 期間の前後比較込み）**。最低 7 日の根拠:
  我々の書き込みレートで autovacuum の発火を複数回・バックアップの日次周期・probe の日内変動を
  各複数回含むこと。営業日変動のない合成負荷のため週次の季節性は考慮不要
- 採取先は DB 内観測テーブル・間隔は 5 分（pgstattuple のみ 1 時間）（§5-3 に根拠と観測者効果）
- teardown は 9/16（§9。マージン 2 日の根拠付き）
